apiVersion: v1
kind: Service
metadata:
  name: uwiyuere
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uwiyuere"
    port: 8080
  selector:
    app: uwiyuere
