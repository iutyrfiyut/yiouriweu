apiVersion: v1
kind: Service
metadata:
  name: gdfewe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gdfewe"
    port: 8080
  selector:
    app: gdfewe
