apiVersion: v1
kind: Service
metadata:
  name: gonenorthgames
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gonenorthgames"
    port: 8080
  selector:
    app: gonenorthgames
