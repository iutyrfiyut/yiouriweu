apiVersion: v1
kind: Service
metadata:
  name: donatelifenm
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "donatelifenm"
    port: 8080
  selector:
    app: donatelifenm
