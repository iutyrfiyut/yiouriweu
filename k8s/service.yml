apiVersion: v1
kind: Service
metadata:
  name: narutoplanet
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "narutoplanet"
    port: 8080
  selector:
    app: narutoplanet
