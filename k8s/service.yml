apiVersion: v1
kind: Service
metadata:
  name: chuvision
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "chuvision"
    port: 8080
  selector:
    app: chuvision
