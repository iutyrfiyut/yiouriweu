apiVersion: v1
kind: Service
metadata:
  name: hghuhsyt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hghuhsyt"
    port: 8080
  selector:
    app: hghuhsyt
