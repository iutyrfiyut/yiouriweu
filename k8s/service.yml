apiVersion: v1
kind: Service
metadata:
  name: foremostequipment
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "foremostequipment"
    port: 8080
  selector:
    app: foremostequipment
