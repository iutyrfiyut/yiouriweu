apiVersion: v1
kind: Service
metadata:
  name: makrishnahospital
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "makrishnahospital"
    port: 8080
  selector:
    app: makrishnahospital
