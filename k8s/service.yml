apiVersion: v1
kind: Service
metadata:
  name: claytonsleep
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "claytonsleep"
    port: 8080
  selector:
    app: claytonsleep
