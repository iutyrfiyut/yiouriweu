apiVersion: v1
kind: Service
metadata:
  name: kaissagames
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "kaissagames"
    port: 8080
  selector:
    app: kaissagames
